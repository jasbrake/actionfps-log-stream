{
  "name": "actionfps-log-stream",
  "version": "0.0.1",
  "description": "Streams the logs served by ActionFPS using EventSource to a file.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jason Brake",
  "license": "ISC",
  "dependencies": {
    "eventsource": "^1.0.0"
  }
}
